import { Invitation } from '@/components/Invitation';

export const metadata = {
  title: 'Bar Mitsva Chmouel — La Soirée',
};

export default function Soiree() {
  return <Invitation variant="soiree" />;
}
